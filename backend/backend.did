type BlogPost = 
 record {
   content: text;
   id: nat;
   timestamp: int;
   title: text;
 };
service : {
  createPost: (text, text) -> (nat);
  getJobs: () -> (vec text) query;
  getPosts: () -> (vec BlogPost) query;
}
